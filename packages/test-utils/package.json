{
  "name": "@dispatch/test-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared test helpers for Dispatch workspaces (foundation-level only: health-response assertions). No business-scenario fixtures.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@dispatch/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
