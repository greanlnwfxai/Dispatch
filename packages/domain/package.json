{
  "name": "@dispatch/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-independent domain foundation for Dispatch (branded identifier helper only). No aggregates, no business rules \u2014 those are introduced module-by-module in future milestones.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
