{
  "name": "@dispatch/validation",
  "version": "0.1.0",
  "private": true,
  "description": "Generic validation foundation for Dispatch (assertion helpers only). No business/validation rules (BR-xxx/VR-xxx) are encoded here.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
